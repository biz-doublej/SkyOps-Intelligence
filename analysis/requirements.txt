# NOTE:
# `analysis/requirements*.txt` and `pipeline/requirements.txt` must not be
# installed into the same Python environment.
# The analysis stack uses newer `numpy` / `pyarrow`, while `apache-flink`
# pulls in `apache-beam`, which pins older versions and conflicts.
# Recommended:
#   python -m venv .venv-analysis
#   .\.venv-analysis\Scripts\Activate.ps1
#   python -m pip install -U pip
#   pip install -r analysis/requirements.txt -r analysis/requirements_ml.txt
#
# ── 데이터 처리 ────────────────────────────────────────────
pandas==2.2.2
numpy==1.26.4
pyarrow==16.1.0        # parquet 저장 (대용량 최적화)

# ── 시각화 ─────────────────────────────────────────────────
matplotlib==3.9.0
seaborn==0.13.2

# ── 머신러닝 ───────────────────────────────────────────────
scikit-learn==1.5.0

# ── Kaggle 데이터셋 다운로드 ────────────────────────────────
kaggle==1.6.14

# ── 유틸리티 ───────────────────────────────────────────────
tqdm==4.66.4
python-dotenv==1.0.0
