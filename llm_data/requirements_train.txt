# SkyOps Intelligence - Week 8 QLoRA training dependencies
#
# 1. Install a CUDA-enabled PyTorch build first.
#    Use the official selector: https://pytorch.org/get-started/locally/
# 2. Then install:
#    pip install -r llm_data/requirements_train.txt
#
# Notes:
# - `bitsandbytes` official wheels support Windows x86-64 for NVIDIA CUDA.
# - `meta-llama/Llama-3.1-8B-Instruct` is a gated model. Set `HF_TOKEN` or
#   login with `huggingface-cli login` before training.

transformers>=4.46.0
datasets>=3.0.0
accelerate>=0.34.0
peft>=0.13.0
trl>=0.11.0
bitsandbytes>=0.49.2
wandb>=0.17.0
sentencepiece>=0.2.0
scipy>=1.13.0
