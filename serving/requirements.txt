# =====================================================================
#  SkyOps Intelligence — Serving 모듈 의존성
#  pip install -r serving/requirements.txt
# =====================================================================

# ── Web Framework ─────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# ── ML 모델 (추론) ────────────────────────────────────────────────────
xgboost>=2.0.0
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.1.0

# ── LLM / RAG ─────────────────────────────────────────────────────────
langchain>=0.2.0
langchain-openai>=0.1.0
langchain-community>=0.2.0
langchain-core>=0.2.0

# ── 벡터 DB / 임베딩 ──────────────────────────────────────────────────
chromadb>=0.5.0
sentence-transformers>=3.0.0

# ── Quantization / LLM 서버 (학습·서빙 환경) ──────────────────────────
# vLLM은 CUDA 빌드가 필요하므로 별도 설치
# pip install vllm
# pip install autoawq

# ── Fine-tuning / 병합 (01_merge_model.py) ────────────────────────────
# transformers>=4.40.0
# peft>=0.10.0
# torch>=2.2.0
# accelerate>=0.28.0

# ── Prometheus 메트릭 ─────────────────────────────────────────────────
prometheus-fastapi-instrumentator>=6.1.0

# ── 기타 유틸 ─────────────────────────────────────────────────────────
httpx>=0.27.0        # 비동기 HTTP (테스트용)
python-dotenv>=1.0.0 # 환경 변수 관리
